services:
  - type: web
    name: inventory-backend
    env: node
    region: frankfurt
    plan: free
    buildCommand: |
      cd server &&
      npm install &&
      npx prisma generate &&
      npx prisma migrate deploy --verbose &&
      npx prisma db seed &&
      npm run build

    startCommand: |
      cd server &&
      node dist/src/index.ts
    envVars:
      - key: DATABASE_URL
        sync: false